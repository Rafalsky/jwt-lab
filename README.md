# jwt-lab
 Proof of Concept repo for understanding token-based authentication and authorization in practice: JWT (JWS), sessions, opaque tokens, refresh tokens, and token introspection — including browser threats (XSS/CSRF) and delivery strategies (HttpOnly cookies vs Authorization header).
